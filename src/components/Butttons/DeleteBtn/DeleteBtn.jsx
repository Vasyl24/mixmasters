import { StyledDeleteBtn, TrashIcon} from './DeleteBtn.styled'

export const DeleteBtn = ({ id, page }) => {
    return (
        <>
        <StyledDeleteBtn>
         <TrashIcon />
        </StyledDeleteBtn>
        </>
    )
}